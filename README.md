# flux

All files looks fine just for healthcheck i added probe to deployment yaml but but i put it in comment.
Also normally could create 3 replica for each deployment
Also i could add antiaffinity but i was using 1 cluster so we do not needed here.